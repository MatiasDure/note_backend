{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "buildfront": "rm -rf dist && cd ../fullstack_tutorial/ && npm run build && cp -r dist ../backend_part_tutorial",
    "deploy": "npm run buildfront && git add . && git commit -m 'uibuild' && git push origin main"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
